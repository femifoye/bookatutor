<%= form_with(model: [@user, @profile], local: true) do |form| %>
  <% if @profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% @profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :education_level %>
    <%= form.text_field :education_level, id: :student_education_level %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :student_description %>
  </div>

  <div class="field">
    <%= form.label :subjects_of_interest %>
    <%= form.text_area :subjects_of_interest, id: :student_subjects_of_interest %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :student_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
