<%= form_with(model: [@user, @booking, @session], local: true) do |form| %>
  <% if @session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@session.errors.count, "error") %> prohibited this session from being saved:</h2>

      <ul>
      <% @session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start %>
    <%= form.datetime_select :start, id: :session_start %>
  </div>

  <div class="field">
    <%= form.label :end %>
    <%= form.datetime_select :end, id: :session_end %>
  </div>

  <div class="field">
    <%= form.label :members %>
    <%= form.text_area :members, id: :session_members %>
  </div>

  <div class="field">
    <%= form.label :booking_id %>
    <%= form.text_field :booking_id, id: :session_booking_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
